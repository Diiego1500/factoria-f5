{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    {{ parent() }}
    <div class="container">
        <div class="p-4 my-4 bg-white border rounded">
            <h3 class="mt-3">Editar Foto Seleccionada:</h3>
            <hr class="mb-5">
            {% include 'image/image-form-template.html.twig' %}
            <hr>
            <a href="{{ path('list') }}" class="btn btn-primary">Regresar</a>
        </div>
    </div>
{% endblock %}